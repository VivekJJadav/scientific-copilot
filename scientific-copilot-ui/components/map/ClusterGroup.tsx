'use client'

export function ClusterGroup() {
  // This is a placeholder — cluster boundary overlays are implemented
  // via React Flow's Background styling + node grouping in ResearchMap.
  return null
}
